"use strict";

const PORT = 3000;

const getCustomerId = () => '1'; // faking this because building an account provisioning/login system is out of scope

module.exports = {
    PORT,
    getCustomerId
};
